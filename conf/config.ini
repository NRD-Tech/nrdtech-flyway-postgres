[DEFAULT]

flyway.url=jdbc:postgresql://${HOST}:${PORT}/${DB}?currentSchema=${SCHEMA}
flyway.user=${USER}
flyway.password=${PASS}
flyway.locations=filesystem:./migration, filesystem:./static
flyway.placeholderPrefix={{
flyway.placeholderSuffix=}}
